---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["bg-background text-foreground min-h-screen w-full", className]}>
  <div class="relative z-10 p-4 sm:p-8">
    <slot />
  </div>
</div>
